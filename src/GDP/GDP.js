import React from "react";
import Graph from "./Grap";

const GDP = () => {
  return (
    <div className="GDP content flex justify-center" id="GDP">
      <Graph />
    </div>
  );
};

export default GDP;
